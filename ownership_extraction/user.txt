<task>
Extract a flat directed ownership graph of ALL ownership relationships in the document — both directions, no target entity, no direction filtering. Fuse chart_structure (from upstream Chart Reader) and non-chart document text (registers, tables, prose, certificates) into a single graph. Each edge carries source_type = "chart_structure" or "document_text". There is no target entity and no layer filtering.
- The definition of too big is when the number of elements under the 'nodes' field is more than 200.
- Do not truncate the output unless instructed by the presence of submitBatch tool.
</task>

<gcs_document_path>{{gcsDocumentPath}}</gcs_document_path>

<chart_structure>{{chartStructure}}</chart_structure>

<critic_feedback>{{criticFeedback}}</critic_feedback>

<instructions>
CRITICAL PREAMBLE — TWO SOURCES (W1.R12 + W1.R13):
You fuse TWO sources into one flat ownership graph. There is NO target entity and NO direction filtering — record every relationship in both directions.
- chart_structure (if non-empty) is AUTHORITATIVE for the chart's visual content. Build chart-sourced edges mechanically from chart_structure.arrows: owner = arrow.from, owned = arrow.to. Do NOT reverse, invent, or re-read the chart visually. Tag each such edge source_type = "chart_structure" and set direction_proof = "From chart_structure arrow: '<evidence string>'". The percentage comes from the arrow's label_percentage or the owned entity's associated_label, else "Not Available". Arrows with direction_confidence UNKNOWN produce no edge (record in data_gaps); LOW produce an edge with a data_gaps note.
- Non-chart TEXT (shareholder registers, cap tables, certificates, prose) is read DIRECTLY for additional relationships. Create text-sourced edges for ownership stated there. Tag each source_type = "document_text" and set direction_proof to the page/section text proving direction. Nodes may come from text as well as from chart_structure.entities.
- HARD BOUNDARY: never extract entities/edges/percentages from any visual diagram or boxes-and-arrows graphic — chart content comes ONLY from chart_structure. Read only linear text/tables.
- When chart_structure and text describe the SAME relationship, reconcile per W1.R13: union on existence; <=2% take the text value (no conflict); >2% material -> set the edge percentage to "CONFLICT — see conflicts array" and add a conflicts entry with resolution_strategy "deferred", resolved_value "deferred".

CRITICAL PREAMBLE — REJECT GROUP ARROWS (W1.R12 + W1.R10):
The chart_reader is required by CR.R9 to emit ONE arrow PER shareholder, even when the chart visually uses a bracket to group shareholders. If you find an arrow in chart_structure.arrows where:
- arrow.from is a collective noun ("group of individuals", "group of persons", "cluster", "column of shareholders", "the shareholders", etc.) instead of a specific named entity, OR
- arrow.from does NOT match any name in chart_structure.entities,
that arrow is MALFORMED. Do NOT create an edge from it. Do NOT guess which individuals it represents based on horizontal_position or visual proximity — that is the contamination failure mode. Instead:
- Record the malformed arrow in data_gaps with verbatim text: "chart_structure arrow contains group-level 'from' field '<value>' — cannot resolve to individual shareholders. Per W1.R10/CR.R9, no edge created for affected shareholders. Re-run chart_reader with CR.R9 compliance."
- The affected shareholders (entities in chart_structure.entities whose only incoming/outgoing arrow is the malformed group arrow) get nodes but NO edges. Add data_gaps on each affected node noting the chart_reader CR.R9 failure.
- Do NOT fall back to guessing assignments from chart positions, family names, or column alignment. This guarantees contamination. Flagging is correct; guessing is not.

CRITICAL PREAMBLE — HALT ON INCONSISTENT chart_structure (W1.R12 + CR.R10 + CR.R12):
chart_structure includes a top-level `sum_check` object reporting per-target percentage sums and an overall_status flag. Before extracting:
- If chart_structure.sum_check.overall_status == "INCONSISTENT", or chart_structure.sum_check.flagged_targets is non-empty, the upstream chart_reader has self-reported branch contamination (one or more holdings' shareholder sums materially deviate from 100%). The chart_structure is NOT trustworthy as ground truth for the flagged targets.
- For each flagged target T in sum_check.flagged_targets: do NOT create edges from shareholders to T. Instead, include T as a node but record on T's data_gaps: "chart_structure.sum_check flagged this target as INCONSISTENT (sum = N%, expected ~100%). No edges created for shareholders of T. Re-run chart_reader (different model or pre-processing) to resolve branch contamination upstream."
- For UNFLAGGED targets, proceed normally per W1.R12.
- INDEPENDENT VERIFICATION: Even when chart_structure says CONSISTENT, recompute per-target sums yourself from chart_structure.arrows. If your computed sum differs from sum_check.per_target_sums entry by >2%, the chart_reader is INTERNALLY INCONSISTENT (reported sum != actual arrow sum) → treat the target as flagged. Record in data_gaps.

BRACKET_MEMBERS VERIFICATION (W1.R12 + CR.R12 — catches compensating-error contamination that sum_check misses):
For each target T that appears as arrow.to of one or more arrows in chart_structure.arrows:
  (i) Collect the set of `bracket_members` arrays across all arrows where arrow.to == T. They MUST all be IDENTICAL (same names, ignoring order). If any two differ → CR.R12 CONSISTENCY VIOLATION → add T to your internal flagged_targets list, do NOT create edges for T, record on T's data_gaps: "chart_structure.arrows pointing to T have inconsistent bracket_members lists (CR.R12 violation). Re-run chart_reader."
  (ii) Collect the set of `from` values across all arrows where arrow.to == T. This set MUST exactly equal the bracket_members set. If any name appears in bracket_members but no arrow with that from exists → CR.R12 COVERAGE VIOLATION (missing shareholder). If any arrow's from is not in bracket_members → CR.R12 violation (extra shareholder). Flag T and skip edges.
  (iii) Check sum_check.per_target_sums[T].shareholder_count against len(bracket_members). If they differ → CR.R12 violation. Flag T.
  (iv) Scan each bracket_members entry for forbidden placeholders ("and N others", "and N other individuals", "...", "etc.", "group", "cluster", "column", "the rest", "others") or any string not matching an entity name in chart_structure.entities. Any such hedge → CR.R12 violation. Flag T.

The bracket_members check catches the failure mode where compensating arithmetic errors make sum_check pass but assignments are still wrong (e.g., one holding has +20% from a misassigned shareholder while an adjacent holding has -20% from the same misassignment, making both sums coincidentally close to 100%). This contamination is invisible to sum_check alone but produces inconsistent bracket_members across siblings.

- Do NOT silently propagate INCONSISTENT chart_structure into the extracted graph. Better to emit fewer edges with a clear data_gaps trail than to commit to wrong assignments.

CRITICAL PREAMBLE — STRICT PER-SHAREHOLDER ARROW USAGE (W1.R10):
For every shareholder you place under a holding entity, the edge MUST be backed by a chart_structure.arrows entry where arrow.from == that specific shareholder's exact name AND arrow.to == that specific holding entity. If no such arrow exists in chart_structure.arrows, do NOT create the edge. Do NOT infer the assignment from horizontal_position. Do NOT infer from name similarity (shared surnames). Do NOT infer from visual proximity.
- For each holding entity, derive its shareholders STRICTLY by filtering chart_structure.arrows for entries where arrow.to == this holding. The set of valid shareholders for this holding is EXACTLY {arrow.from for arrow in that filtered set}.
- Cross-branch contamination check: if a shareholder name appears as arrow.from to MULTIPLE different holding entities in chart_structure.arrows, that shareholder owns stakes in multiple holdings (create separate edges, one per holding). If a shareholder name appears as arrow.from to exactly ONE holding, that shareholder belongs to EXACTLY that branch — they do NOT belong to any other branch regardless of column position or surname.
- Per-holding sum check: after deriving each holding entity's shareholders from chart_structure.arrows, sum their percentages. If the sum is not approximately 100% (±5%), do NOT silently accept. Record in data_gaps: "Holding entity X shareholder sum = N% per chart_structure.arrows — does not approximate 100%. Possible chart_reader CR.R10 failure or genuinely partial chart info." Do NOT attempt to "fix" the sum by reassigning shareholders across branches. Re-running chart_reader is the correct fix.

0. CRITIC FEEDBACK — If critic_feedback above is non-empty, a previous extraction attempt was reviewed and rejected. Read every CRITICAL and IMPORTANT correction carefully. These are specific errors you made last time — missing entities, wrong direction, wrong percentages, person-owns-person edges, etc. You MUST address every correction in this attempt. Do not repeat the same mistakes.
1. Classify entity type from documentary evidence (Category 1, 2, or 3; or "Entity Type not confirmed"). This step uses document text outside the chart (certificates, registration filings, prose).

SINGLE EXTRACTION FLOW (no modes — chart_structure is consumed, never re-read):

2. CONSUME chart_structure — use chart_structure.entities as the chart entity inventory. Build chart-sourced edges mechanically from chart_structure.arrows (owner = arrow.from, owned = arrow.to), applying the group-arrow rejection, UNKNOWN/LOW confidence, bracket_members and sum_check checks above. Tag each such edge source_type = "chart_structure".

3. READ NON-CHART TEXT — read shareholder registers, cap tables, certificates, and prose (NOT any diagram). Create text-sourced edges for ownership relationships stated there. Tag each source_type = "document_text" with a page/section citation in source and direction_proof. Apply W1.R9 (no person-owns-person).

4. MERGE + RECONCILE — combine chart-sourced and text-sourced edges per W1.R13 (union on existence; <=2% take text; >2% material conflict -> deferred).

5. STRUCTURAL CYCLES — run W1.R7 DFS over the merged edge graph.

EMPTY chart_structure: if <chart_structure> is empty, extract from text only. If a chart is visibly present but chart_structure is empty, do NOT read it — add data_gaps "chart present but chartStructure empty; chart_reader may have failed; chart-sourced ownership not extracted."

6. For each edge, record: exact owner id, exact owned id, exact % as stated (or "Not Available"), evidence of control, exact source reference, source_type ("chart_structure" or "document_text"), and direction_proof (cite chart_structure arrow evidence or page/section text).
7. For each node, record: id slug, legal name exactly as stated, entity type, and classification_reasoning (entity type reasoning and proof page/section). For listed companies only, populate listing_proof on the node.
8. Before expanding each entity's owners during DFS cycle detection, check if its id is already in the current path. If yes, record the cycle in the cycles array and stop — do not extract further from that entity.
9. Identify conflicts (same owner→owned relationship, contradicting % values within this document, or chart_structure vs document_text disagreement >2%). Record in conflicts with conflict_description summarizing the disagreement, resolution_strategy "deferred", and resolved_value "deferred".
10. VALIDATION — before writing JSON:
 (a) No edge has both owner and owned as Natural Person (W1.R9).
 (b) Every node appears in at least one edge OR is recorded with a data_gaps note explaining why it is isolated.
 (c) Every chart_structure-sourced node matches an entry in chart_structure.entities by name; every chart_structure-sourced edge matches a chart_structure.arrows entry (owner=from, owned=to). Remove chart-sourced nodes/edges that fail this.
 (d) Every edge has source_type set. chart_structure edges have direction_proof starting "From chart_structure arrow: "; document_text edges cite a page/section.
 (e) Cycles recorded per W1.R7. Conflicts recorded per W1.R13 with resolution_strategy "deferred".
 (f) No layer, ownership_chain, client_entity, or top-level entity_type_category fields are emitted.
11. Reason through all steps above before writing the JSON. Your reasoning is not included in the output.
12. Return strictly valid JSON matching the provided schema. No commentary, no markdown, no explanation outside the JSON.
13. If the submitBatch tool is available and your nodes array exceeds 200 elements, split the output into batches via submitBatch with primaryField "nodes". Each batch must be valid JSON matching the schema, containing a subset of nodes with their corresponding edges, cycles, and conflicts. Preserve node id slugs as-is across batches. After the last batch, return ONLY a text summary — no JSON.
</instructions>
