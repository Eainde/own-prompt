<task>
Extract who OWNS the target entity — the upward ownership chain only. Trace from the target entity up to its owners, their owners, and so on until you reach natural persons or top parents. Do NOT extract subsidiaries, children, or investees of the target entity.
- The definition of too big is when the number of elements under the 'nodes' field is more than 200.
- Do not truncate the output unless instructed by the presence of submitBatch tool.
</task>

<gcs_document_path>{{gcsDocumentPath}}</gcs_document_path>

<target_entity>{{clientEntityName}}</target_entity>

<critic_feedback>{{criticFeedback}}</critic_feedback>

<instructions>
0. CRITIC FEEDBACK — If critic_feedback above is non-empty, a previous extraction attempt was reviewed and rejected. Read every CRITICAL and IMPORTANT correction carefully. These are specific errors you made last time — missing entities, wrong direction, wrong percentages, person-owns-person edges, etc. You MUST address every correction in this attempt. Do not repeat the same mistakes.
1. Classify entity type from documentary evidence (Category 1, 2, or 3; or "Entity Type not confirmed").
2. ENTITY INVENTORY — Before creating any nodes or edges, scan the entire document and list every distinct entity (corporate entities AND natural persons) mentioned in ownership structures. Include intermediate holding companies, investment vehicles, and all persons with ownership percentages. This inventory ensures no entity is missed. For each entity, note which other entity it connects to and the percentage shown. BRANCH MAPPING: After the inventory, group entities by branch — for each direct owner (layer-1 entity) of the target, list which persons/entities are its shareholders. Look closely for vertical or horizontal brackets grouping names together. Trace the line from that specific bracket down to the specific company it points to. Assign all names within that bracket exclusively to that company. DO NOT mix names across brackets. Verify each shareholder is assigned to the correct entity before creating nodes.
3. MANDATORY DIRECTION TABLE — Before creating ANY nodes or edges, build this table for every entity in your inventory from step 2. For each entity, trace the literal arrows in the document to determine direction relative to the target:

   | Entity Name | OWNER or SUBSIDIARY | Evidence |
   For each entity, ask:
   "Does the arrow point FROM this entity TOWARDS the target (directly or via intermediaries)? → OWNER."
   "Does the arrow point AWAY FROM the target TOWARDS this entity? → SUBSIDIARY."
   An arrow pointing from A to B means A owns B. Do NOT assume chart position (top/bottom) implies direction. If an arrow points DOWN from Company A to Company B, it means Company A OWNS Company B. Company B is a SUBSIDIARY and must be excluded.
   ONLY entities classified as OWNER in this table may become nodes in your output. Every entity classified as SUBSIDIARY is permanently excluded — no nodes, no edges, no ownership_chain entries. Do not revisit this classification later.

4. Trace ownership layer by layer UPWARD from the target entity, using ONLY entities classified as OWNER in your direction table from step 3. For each owner at layer N, repeat the direction test to find its owners at layer N+1. For each owner, create a node and an edge. Think through each branch fully before moving to the output.
5. For each edge, record: exact owner id, exact owned id, exact % as stated (or "Not Available"), evidence of control, exact source reference, and direction_proof (why this edge direction is correct and which page proves it).
6. For each node, record: id slug, legal name exactly as stated, entity type, layer, ownership_chain (the full path of ids from the target entity to this node), and classification_reasoning (why this entity is an owner, how the layer was determined, entity type reasoning, and proof page/section). For listed companies only, populate listing_proof on the node.
7. Before expanding each entity's owners, check if its id is already in the current ancestry path. If yes, record the cycle in the cycles array and stop — do not extract further from that entity.
8. Identify conflicts (same owner→owned relationship, contradicting % values within this document). Record in conflicts with conflict_description summarizing the disagreement, resolution_strategy "use_higher", and resolved_value set to the higher %.
9. VALIDATION — before writing JSON: (a) For each node, cross-check against your direction table from step 3. If that entity was classified as SUBSIDIARY, remove it immediately — it does not belong in the output regardless of how the edges are structured. (b) The target entity (layer 0) must NEVER appear in the "owner" field of any edge. (c) No ownership_chain may include ids of entities classified as SUBSIDIARY in your direction table. (d) Layer 1 entities must be direct owners of the target — verify each one: does the document show this entity holding shares IN the target? If the document shows the target holding shares in this entity, it is a subsidiary — remove it. (e) No edge has both owner and owned as Natural Person — if found, re-examine which corporate entity those persons actually own. (f) Cross-check your entity inventory from step 2 — every entity classified as OWNER in your direction table should appear in your nodes; if any are missing, add them.
10. Reason through all steps above before writing the JSON. Your reasoning is not included in the output.
11. Return strictly valid JSON matching the provided schema. No commentary, no markdown, no explanation outside the JSON.
12. If the submitBatch tool is available and your nodes array exceeds 200 elements, split the output into batches via submitBatch with primaryField "nodes". Each batch must be valid JSON matching the schema, containing a subset of nodes with their corresponding edges, cycles, and conflicts. Preserve node id slugs as-is across batches. After the last batch, return ONLY a text summary — no JSON.
</instructions>
