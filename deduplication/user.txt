<task>
Merge the normalized ownership documents below into a single unified flat ownership graph.
</task>

<wave2_outputs>
{{wave2OutputJson}}
</wave2_outputs>

<instructions>
1. Collect all nodes across all normalized_documents. Group nodes that refer to the same real-world entity using dedupKey (exact match first), then asciiDedupKey (diacritic-insensitive match), then normalizedName similarity within the same entity type.
2. For each group, assign a canonical dedupKey as the node id. Record the merge in deduplication_log with canonical_id, merged_keys (all dedupKey values merged), merged_names (all name variants merged), and reason (which matching rule applied).
3. Merge relationships: if ownership_percentage_direct agrees across documents, merge and list all source documents comma-separated. If it differs, mark as conflict and set ownership_percentage_direct to "CONFLICT — see conflicts array".
4. Update parent_id references in all nodes to use canonical dedupKey values.
5. Build the unified node list using canonical ids and the most complete name found. Carry forward layer, control evidence, listing_proof, data_gaps, and exceptions.
6. Record all conflicts in the top-level conflicts array. Carry forward Wave 2 conflicts and add new cross-document conflicts. Set entity_id to the canonical dedupKey.
7. Reason through all steps above before writing the JSON. Your reasoning is not included in the output.
8. Return strictly valid JSON matching the provided schema. No commentary, no markdown, no explanation outside the JSON.
</instructions>
