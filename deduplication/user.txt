<task>
Merge the normalized ownership documents below into a single unified flat ownership graph.
</task>

<wave2_outputs>
{{wave2OutputJson}}
</wave2_outputs>

<instructions>
1. Collect all nodes across all normalized_documents. Group nodes that refer to the same real-world entity using dedupKey (exact match first), then asciiDedupKey, then normalizedName similarity within the same entity type.
2. For each group, assign a canonical dedupKey as the node id. Record the merge in deduplication_log with canonical_id, merged_keys, merged_names, and reason.
3. Collect all edges across all documents. Update each edge's owner and owned to use the canonical dedupKey of the merged node (replacing any pre-merge slug ids).
4. Merge edges: if the same (owner, owned) pair appears with the same % across documents, merge into one edge with comma-separated source. If different %, add a conflict entry (resolution_strategy: use_higher, resolved_value: the higher %) and set the edge's ownership_percentage_direct to "CONFLICT — see conflicts array".
5. After all merges, run DFS over the edge graph (traversing owned → owner direction). Record any newly discovered cycles with detected_at "W3". Carry forward all upstream cycles unchanged.
6. Carry forward all Wave 1/2 conflicts, updating owner/owned to canonical dedupKeys where needed. Add new cross-document conflicts from step 4.
7. Reason through all steps above before writing the JSON. Your reasoning is not included in the output.
8. Return strictly valid JSON matching the provided schema. No commentary, no markdown, no explanation outside the JSON.
</instructions>
