<task>
Merge the normalized ownership documents below into a single unified flat ownership graph.
- The definition of too big is when the number of elements under the 'nodes' field is more than 200.
- Do not truncate the output unless instructed by the presence of submitBatch tool.
</task>

<normalised_entities>
{{normalisedEntities}}
</normalised_entities>

<instructions>
1. Set client_entity to the client_entity value from the input documents. Set entity_type_category from the input documents (all documents share the same value).
2. Collect all nodes across all normalized_documents. Group nodes that refer to the same real-world entity using dedupKey (exact match first), then asciiDedupKey, then normalizedName similarity within the same entity type.
3. For each group, assign a canonical dedupKey as the node id. Record the merge in deduplication_log with canonical_id, merged_keys, merged_names, and reason.
4. Collect all edges across all documents. Update each edge's owner and owned to use the canonical dedupKey of the merged node (replacing any pre-merge slug ids). Carry forward direction_proof on each edge unchanged. Apply the same ID remapping to every entry in each node's ownership_chain array. Carry forward classification_reasoning on each node; when merging, keep the most detailed.
5. Merge edges: if the same (owner, owned) pair appears with the same % across documents, merge into one edge with comma-separated source. If different %, add a conflict entry (conflict_description summarizing the disagreement, resolution_strategy: use_higher, resolved_value: the higher %) and set the edge's ownership_percentage_direct to "CONFLICT — see conflicts array".
6. After all merges, run DFS over the edge graph (traversing owned → owner direction). Record any newly discovered cycles with detected_at "W3". Carry forward all upstream cycles unchanged.
7. Carry forward all Wave 1/2 conflicts, updating owner/owned to canonical dedupKeys where needed. Add new cross-document conflicts from step 5.
8. Reason through all steps above before writing the JSON. Your reasoning is not included in the output.
9. Return strictly valid JSON matching the provided schema. No commentary, no markdown, no explanation outside the JSON.
10. If the submitBatch tool is available and your nodes array exceeds 200 elements, split the output into batches via submitBatch with primaryField "nodes". Each batch must be valid JSON matching the schema, containing a subset of nodes with their corresponding edges, cycles, and conflicts. Preserve node id slugs as-is across batches. After the last batch, return ONLY a text summary — no JSON.
</instructions>
