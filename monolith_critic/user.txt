<task>
Review the monolith ownership extraction output against the source documents and the extraction rules. Evaluate all 17 acceptance criteria and return a single JSON object, strictly conforming to the provided schema, with structured per-criterion feedback.
</task>

<extracted_records>
{{extractedRecords}}
</extracted_records>

<gcs_document_path>{{gcsDocumentPaths}}</gcs_document_path>

<client_entity_name>{{clientEntityName}}</client_entity_name>

<adoption_locations>{{adoptionLocations}}</adoption_locations>

<adverse_media>{{adverseMedia}}</adverse_media>

<entity_type>{{entityType}}</entity_type>

<due_diligence_level>{{dueDiligenceLevel}}</due_diligence_level>

<document_classifications>{{documentClassifications}}</document_classifications>

<kos_documents>{{kosDocuments}}</kos_documents>

<verification_dates>{{verificationDates}}</verification_dates>

<is_streamlined>{{isStreamlined}}</is_streamlined>

<instructions>
1. COMPLETENESS (primary, "Entity Completeness") — FIRST apply the batching guard: the extractor treats more than 50 extracted_records elements as too big and may emit the set in batches, so if the ids do not start at 1 and run contiguously to N, or the set is truncated with the structure obviously continuing, you have a partial batch — say so, score this criterion Mixed_Partial_Pass_Partial_Fail, and raise no CRITICAL missing-entity findings for parties that would fall in a later batch. Then apply the SCOPE GUARD: the review is of the CLIENT'S UPWARD OWNERSHIP STRUCTURE, not of the documents' contents. IN SCOPE is the client anchor plus every party the documents evidence as holding a directed ownership or control path TO the client, directly or through intermediates - including every co-shareholder of every in-scope entity, at every layer, however small. OUT OF SCOPE is everything else: the client's own subsidiaries, sister and affiliate companies, and parties bearing no evidenced ownership relation to the structure. An out-of-scope party is correctly absent from extracted_records AND from qaFlags.records; naming it in outOfBounds.summary is the complete and correct treatment (OC.2), and demanding more is a false finding. A party whose relation to the client the documents do not establish is UNRESOLVED, not out of scope, and must still be accounted for. Then read the source documents at gcs_document_path (and kos_documents, where provided). List every IN-SCOPE person, organization and org-chart box/node they contain. Verify each one appears as a record in extracted_records, matching by PARTY IDENTITY under rule 3's formatting tolerances (punctuation, case, spacing, legal suffix, brackets, accents, transliteration) — NOT by string equality on nameAsSource. Before reporting any party missing, scan every nameAsSource and linkedName in the record set for a NEAR-IDENTICAL name (shared stem or distinctive token, or a few characters apart — "Molehill Holding B.V." against "Mollie Holding B.V."): where one exists it is NOT a missing entity, and you must re-read the page to decide whether it is the same party under the spelling the document prints (no finding — your transcription was wrong) or two distinct parties (a naming finding quoting BOTH strings, never a CRITICAL missing entity). A party you name or cite by id anywhere else in your own response is present, and cannot be reported absent. Verify also that no out-of-scope party is emitted as a record with isOutOfBounds = false (that is over-inclusion: IMPORTANT, not CRITICAL). When summing a target's shareholders for the §9.3 check, count each distinct HOLDER once — rule 14 retains multiple versions of one conflicting link, and adding them together fabricates an over-100% breach. Any IN-SCOPE entity in the documents but missing from extracted_records is a CRITICAL Entity Completeness failure — name it and cite where it appears. Do not accept an omission on the grounds that diluted/indirect ownership falls below a threshold; every direct link must be preserved regardless of the diluted result.

2. PERCENTAGE ACCURACY (primary, "Percentage Accuracy") — For each record, check actualOwnershipPercentage (and actualVotingRightsPercentage where the document states it) against the source. It must match EXACTLY — no rounding, no assumption. A percentage that is stated in a document but recorded differently, or a percentage invented where the document gives none, is a CRITICAL Percentage Accuracy failure — cite the document value and the recorded value. Two wordings ARE percentage evidence and must not be read as "the document gives none": rule 9.2's wholly-owned wordings, and rule 9.2.1's abbreviated "WO" code where the document's legend, relationship column or adjacent heading resolves it for this owner-subsidiary pair — both yield 100, and scoring a resolved "WO" 100 as invented is a false finding. Recording a resolved "WO" as 0 with MISSING_PERCENTAGE, or treating it as an ORBIS "MO" (which evidences only more than 50%, rule 6), is the CRITICAL failure in the other direction.

3. HALLUCINATION / NO EXTERNAL KNOWLEDGE / EVIDENCE GROUNDING ("No External Knowledge / Hallucination") — Confirm every record, every linkedName link, and every percentage traces to an explicit statement in gcs_document_path or kos_documents. Nothing may be sourced from internet lookups, public databases, or general knowledge, even if a URL appears in a document. Additionally apply the evidence-grounding rule EXACTLY as stated under "Structural Validation" in system.txt — do not restate a looser version of it here. Any record asserting a NON-ZERO actualOwnershipPercentage, a NON-ZERO actualVotingRightsPercentage, or a non-null linkedName must carry a non-null verbatim evidenceSnippet AND a numeric pageNumber. A null evidenceSnippet is permitted ONLY where all three of these hold: linkedName is also null (the layer-0 client record), AND the record asserts no percentage, AND the record carries a MISSING_PERCENTAGE / SOURCE_DATA_NOT_AVAILABLE gap. (The percentage fields are numeric and are never null — missing is emitted as 0 plus the flag — so test them at zero, not at null.) A null snippet outside that three-condition case is a grounding failure. Flag anything not traceable to a provided document as CRITICAL.

4. STRUCTURAL ("Structural Validation") — Apply the R1 direction contract from system.txt throughout. Confirm EVERY non-null linkedName resolves to an existing record's nameAsSource — there is no exception, and isUltimateParent is not one (an ultimate parent still names the entity below it in linkedName). The only record permitted a null linkedName is the layer-0 client. Confirm every percentage field falls within 0-100 and every field the schema requires is present. Confirm layer is internally consistent: the client at layer 0, and every other record's layer equal to the layer of the record for its linkedName target, plus 1 — an owner sits one layer FURTHER from the client than the entity it owns. Confirm relationshipType comes from the R4 enum (null only on the client), roleCapacity is one of the 22 R2 values, coverageMode is "ALL", and a roleCapacity of "Listed Parent" (or a listed-entity stop in controlsApplied) is paired with a non-null listingProof. Confirm the R8 container shapes: top-level clientAnchor / outOfBounds / qaFlags / advisory emitted as JSON objects even when empty, per-record outOfBounds an object, per-record qaFlags an array. Confirm outOfBounds entry shapes: documents[] keyed by fileName with status DOC_INVALID or DOCUMENT_NOT_RELEVANT; records[] with a required integer id, the gap token (e.g. SOURCE_DATA_NOT_AVAILABLE) at the START of reason, and status NEED_REVIEW or COUNTRY_OUT_OF_SCOPE — the gap token is never a status value. Confirm canonical record order using the rule's own collation (NFC-normalized, case-folded, Unicode code-point order, raw-value tie-breaks) — do not raise an ordering finding from a naive or locale comparison. Confirm no R7 working-state key leaked into the output — the schema permits additional properties, so this check is the only thing catching one. Confirm governanceBasis carries all eight R5 parts in order, with part [1]'s trace tokens before Classification, and part [1] Classification equal to part [7] FinalOwnershipClassification. Each of these is IMPORTANT on failure.

5. REMAINING CHECKS — Evaluate the remaining criteria to the extent the inputs supplied to you support (all are schema-backed):
   - "Client Anchor Validation" — verify the top-level clientAnchor object: providedName echoes client_entity_name, resolvedName/clientRecordId point to the layer-0 record the set is anchored to, validationStatus is consistent with that match, and ownershipReviewStatus reflects the actual outcome. Schema-backed; a validationStatus/ownershipReviewStatus inconsistent with the evidence is IMPORTANT, and a record set anchored to a different entity than providedName is CRITICAL.
   - "Ownership Direction" — apply R1: nameAsSource is the OWNER and linkedName is the entity it owns, one layer closer to the client, so each record reads "nameAsSource is a [relationshipType] of linkedName". Check that reading against the documents. A record whose linkedName names the subject's OWNER is inverted (IMPORTANT); a record pointing at an entity it owns is CORRECT and must not be scored as an inversion. Direction must rest on shareholder tables, percentages, registry wording, ownership statements, chart legends or explanatory notes — never on visual placement, arrow orientation, SmartArt layout, OCR order or chart proximity. Where a Natural Person and a Non-Natural Person are linked the person is the owner unless the documents evidence the reverse, so a Natural Person appearing as another record's linkedName needs explicit evidence. Where direction cannot be confirmed the extractor must emit OWNERSHIP_DIRECTION_REVIEW_REQUIRED and take ladder rung 2 "Unknown / Needs Review" rather than emit a presumed edge: an edge resting on chart position alone is CRITICAL (fabricated link), and an unresolved-direction record classified anyway is IMPORTANT.
   - "Source Classification & Admissibility" — verify the per-record sourceClass (H1/H2/H3) is populated wherever determinable and matches the controlling source's actual class. document_classifications carries the PROVIDER's label, and rule 6 requires classification from the DOCUMENT itself — so judge sourceClass against what the document is, not against the provided label: a document-derived class that diverges from document_classifications, with the divergence recorded in governanceBasis part [2], is CORRECT; silently adopting the provided label, or silently discarding it, is IMPORTANT. Also verify that stronger-class evidence is not silently overridden by weaker evidence, and no evidence was dropped. Orbis / ultimate-owner attribution control (section 6): an Orbis-style label alone (GUO, GUP, Ultimate Owner, Ultimate Parent, Parent Company, Group Parent, Head of Group) is an attribution, not ownership evidence — verify no record derives a threshold-bearing edge, percentage, or IBO/UBO classification from such a label without independent ownership documentation (percentages, ownership chain, shareholder register, annual-report ownership section, org/structure chart, regulatory filing, or official company documentation); unsupported attributions must be context-only (roleCapacity "Information Only" / data gap). Schema-backed via sourceClass; IMPORTANT on failure.
   - "Methodology (Global vs Streamlined)" — cross-check is_streamlined and adoption_locations against the top-level ownershipApproach field (GLOBAL/STREAMLINED/MIXED/NEED_REVIEW): if streamlined, confirm it was permitted and justified (controlsApplied, listingProof); if not, confirm the extraction does not look prematurely truncated. Section 8.3 (KOS 8.0a): where ownershipApproach = STREAMLINED the Domination Methodology applies ALONE - verify no percentages were multiplied across layers and no cumulative indirect figure was computed, that drill-down continued only through dominating links (rule 10.2.0 D1/D2/D3), that each dominating owner INHERITS the dominated entity's full position in the client (dilutionOwnershipPercentage = the deemed inherited position, never a product) with dominationIndicator = YES, and that a branch whose owner does not dominate carries no inheritance and stops. A streamlined record set showing diluted products (70% x 60% recorded as 42%), or mixing dilution and domination within one path, is a failure. Schema-backed via ownershipApproach; IMPORTANT on failure.
   - "Local Overlay Applied" — using adoption_locations, verify countryProfileApplied reflects any stricter local threshold, countryOverrideNote explains any override of the global default, the per-record localOverlayApplied / thresholdApplied / inclusiveThreshold / drillDownRequired fields are set correctly for affected records, AND the classification outcome reflects the local threshold (section 13.6): an owner meeting a stricter/inclusive local threshold is classified isUBO/isIBO accordingly and not down-classified for being below the global default or at an inclusive boundary (e.g. exactly 25% under ≥25%). Schema-backed; IMPORTANT on failure (an omitted qualifying owner is CRITICAL under Entity Completeness).
   - "IBO / UBO Classification Accuracy" — using due_diligence_level and adoption_locations, verify isIBO / isUBO / roleCapacity against rule 12's definitions and thresholds: GLOBAL IBO &gt; 25% (CDD and EDD); UBO &gt; 25% (CDD) / &gt; 10% (EDD), unless a stricter/inclusive local threshold governs (rules 13/13.6). Thresholds apply to CUMULATIVE (diluted) ownership — the product of direct percentages along the owner's path to the client (rule 10.1 / 12.3) — not a single direct link's actualOwnershipPercentage; verify the diluted computation where the chain is evidenced. Under the STREAMLINED pathway thresholds are instead tested against the DEEMED INHERITED position produced by the Domination Methodology (rule 8.3): a dominating owner inherits the dominated entity's full client position (a 55% holder of an entity holding 70% of the client is a UBO at a deemed 70%, not 38.5%), and a non-dominating owner inherits nothing. Where rule 10.2.0 returned dominationIndicator = YES for a link a party holds, the control-based qualification IS met and isIBO / isUBO must be true — a YES paired with a false boolean and no higher-rung stop rule accounting for it is a failure. No qualifying owner left unflagged, no sub-threshold owner over-classified, control-based UBO counted even below the ownership threshold, and the type invariant respected (Natural Person never isIBO; Non-Natural Person never isUBO). Apply the rule 12.2.1 UBO-qualification gate: a Natural Person with isUBO = true must satisfy at least one qualification criterion (Global threshold, stricter Local threshold, control, domination, voting-rights, or diluted-ownership/cumulative qualification); a Natural Person flagged isUBO solely for appearing in the structure — e.g. an 8% direct owner with no control/domination/voting/diluted basis — is over-classified and should be isUBO = false with a non-UBO roleCapacity. Schema-backed; a wrong flag is IMPORTANT, an omitted qualifying owner is CRITICAL under Entity Completeness.
   - "Domination & Control Indicators" — walk rule 10.2.0's D1-D4 ladder yourself for EVERY record, on both pathways, and score in BOTH directions. dominationIndicator is null only on the layer-0 client; a null elsewhere means the assessment was skipped. A link the documents evidence at more than 50% share ownership or more than 50% voting rights recorded as dominationIndicator = NO is a MISSED domination, not a conservative call. dominationOwnership = actualOwnershipPercentage where domination rests on majority share ownership (D1) and dominationVotingRights = actualVotingRightsPercentage where it rests on majority voting (D2), otherwise 0 (rule 10.2.1): an above-50% link carrying dominationOwnership = 0, a dominationIndicator = YES with both fields 0 AND controlRights null, or either field carrying the deemed inherited position instead of the direct figure, is a failure. governanceBasis part [1] must name the basis in DominationBasis. controlRights, ownershipVotingMismatch and controlBasedUBOAssessmentRequired must be consistent with the cited evidence (no domination concluded without support). Also verify the recorded diluted figures. STREAMLINED CARVE-OUT first: where ownershipApproach = STREAMLINED, dilutionOwnershipPercentage carries the DEEMED INHERITED position and a product is forbidden (rule 8.3.3), so do not score the inherited figure as mis-multiplied - that pathway is checked under Methodology. On the GLOBAL pathway: dilutionOwnershipPercentage equals the product of actualOwnershipPercentage along the record's path to the client (rules 10.1/12.3), computed as a PERCENTAGE-OF-PERCENTAGE chain - (p1/100) × ... × (pn/100) × 100, so 100% links pass the interest through undiluted and `100% × 100% × 52.27% × 100% × 100% = 52.27%` is correct; never recompute a DilutionChain as a bare integer product, and never score its result as nonsensical because the printed operands multiply to a large number, and dilutionVotingRightsPercentage the same product over actualVotingRightsPercentage (rule 10.1.1) — or 0 with a DILUTED_VOTING_INCOMPLETE qaFlag when any link on the path lacks a stated voting % (the flag marks unknown vs a genuine 0, per OC.3). Two prohibitions from rule 10.1.1: voting rights must never be assumed equal to ownership percentages, and the ownership figure must never be substituted for a missing voting figure — a dilutionVotingRightsPercentage set to the ownership product (or any nonzero value) across a path with a missing-voting link, or a 0 emitted WITHOUT the DILUTED_VOTING_INCOMPLETE flag when a voting link is missing, is a failure. Check that flag on EVERY record independently of MISSING_VOTING_RIGHTS: the two name different facts (this link's own unstated figure vs the whole path's product), both are emitted where both hold, and a set carrying MISSING_VOTING_RIGHTS alone while every dilutionVotingRightsPercentage is 0 is exactly the failure this clause describes. Diluted voting is not tested against the ownership-based thresholds but should inform dominationIndicator / controlBasedUBOAssessmentRequired where the chain evidences control (rule 10.2). Schema-backed; IMPORTANT on failure.
   - "Advisory & QA Assessment" — verify the top-level advisory.records contains an entry for each material information gap evidenced in the documents (using verification_dates for document currency and due_diligence_level for context), and that qaFlags are present for material gaps and not over-emitted for routine complexity. Also check disputed findings: a qaFlags.records reason beginning CRITIC_FINDING_DISPUTED is the extractor re-examining a finding from your previous evaluation and reporting that the documents do not support it — a legitimate outcome, not defiance. Re-read the document and page it names: if the documents support the original finding, restate it at its original severity and cite the line that refutes the dispute; if they do not, the extractor was right — Pass, do not re-raise. Every dispute must carry a matching advisory.records entry requesting what would settle it; a dispute with no advisory is IMPORTANT, since the advisory is what ends the retry loop. Also check INPUT-VALIDATION: where a mandatory input (client_entity_name, entity_type, adoption_locations, is_streamlined, ownership-relevant documents, verification_dates where currency must be assessed, or the kos_documents extract a local overlay depends on) was missing or unusable, the extractor must record the gap, flag MISSING_MANDATORY_INPUT where it materially affects completeness, and raise an advisory — never invent the input. Both halves are schema-backed; IMPORTANT on failure, CRITICAL where an input was invented.
   - "Conflict Preservation" — check that where documents conflict on percentage, voting rights, or shareholder list, both versions are preserved via conflictTag rather than silently resolved. Schema-backed; IMPORTANT on failure.
   - "Document Handling & Scope" — (a) every ownership-relevant document INSTANCE supplied to you is accounted for somewhere in the output (documents are not deduplicated by file name); (b) non-English sources were translated before extraction, quoted translated in evidenceSnippet, with translation status in governanceBasis part [5] or a DOCUMENT_TRANSLATION_REQUIRED gap; (c) documents binding to a parent/group/brand rather than the client are routed to outOfBounds.documents and not used as ownership evidence, and outOfBounds.records carries the SOURCE_DATA_NOT_AVAILABLE gaps; (d) circular ownership is preserved without repeated traversal. An out-of-scope document used as evidence, or a skipped source that costs a party, is CRITICAL; the rest IMPORTANT.
   - "Entity-Type Specific Logic" — using entity_type and adverse_media: the listed-entity stop only where all four conditions hold (entity_type = Listed Entity, listing evidence present, validated, anchor-bound, and permitted), with listingProof and controlsApplied recorded, no shareholders emitted above it, and ownershipReviewStatus = COMPLETE — and NOT taken where local rules require drill-down, where adverse_media requires enhanced review, or where listing status is unvalidatable; notional UBO not affirmatively classified without an express Local Addendum requirement, with NOTIONAL_UBO_ASSESSMENT_REQUIRED emitted and rung 12 taken instead; MOS exemption only with all three legs evidenced; every evidenced trust and foundation role captured with the matching relationshipType and rung, and never converted into an invented ownership percentage; GP look-through performed where local rules require it, with NNP_CHAIN_INCOMPLETE where the chain breaks; SPV structures not assumed parent-controlled. Unevidenced stops are CRITICAL; recording failures IMPORTANT.
   - "Non-Ownership Information Suppressed" — verify no record rests on a governance role alone. A person named only as a director, officer, executive, company secretary, registered agent, signatory or contact person, with no evidenced shareholding, membership, partnership, trust/foundation role, voting right or control right, must NOT appear as an ownership link — not as a low-percentage owner, not at 0%, not as an "Information Only" stand-in. Emitting one is a fabricated owner (CRITICAL). Exceptions: a party independently holding an evidenced ownership/control interest is recorded on that basis, and the Senior Most CSM (§11.1.1) is identified by designation only. Where no ownership evidence exists at all, the correct output is the client anchor record alone plus qaFlags.summary = "NO OWNERSHIP EVIDENCE IDENTIFIED IN THE REVIEWED DOCUMENTATION." with ownershipReviewStatus = NEED_REVIEW — a structure assembled from directors and officers instead is CRITICAL.
   - "Advisory & QA Assessment" — before raising or excusing any advisory finding about an unresolved upstream branch, run §15.1's HEADROOM test: the branch ceiling is the terminal owner's dilutionOwnershipPercentage, and where that ceiling cannot meet the applicable UBO threshold AND no §10.2 control basis is evidenced on the branch, no advisory and no NNP_CHAIN_INCOMPLETE are required — demanding them is a false finding. Check governanceBasis part [6] carries HeadroomTest=[...] instead.
   - "Overall Goal Adherence" — a surface-level, holistic check that the output as a whole is a complete, evidence-based ownership structure derived only from the provided documents, consistent with an audit-ready ownership review.

6. SCORE all 17 acceptance criteria (Pass / Fail / Mixed_Partial_Pass_Partial_Fail), using the exact criterion names from system.txt, with grounded observations (what is right/wrong, the document location, and the expected correct value). Score each defect ONCE, under the criterion that owns it, and cross-reference rather than restate it. Where a criterion was only partially assessable from the inputs supplied to you, score Mixed_Partial_Pass_Partial_Fail and name what was missing — never Pass by default, never Fail for something you could not see. Order areas_for_improvement by severity (most critical first) and set the verdict per the verdict_rules in system.txt.
</instructions>
