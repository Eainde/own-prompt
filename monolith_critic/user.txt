<task>
Review the monolith ownership extraction output against the source documents and the extraction rules. Evaluate all 14 acceptance criteria and return a single JSON object, strictly conforming to the provided schema, with structured per-criterion feedback.
</task>

<extracted_records>
{{extractedRecords}}
</extracted_records>

<gcs_document_path>{{gcsDocumentPath}}</gcs_document_path>

<client_entity_name>{{clientEntityName}}</client_entity_name>

<client_entity_type>{{clientEntityType}}</client_entity_type>

<adoption_locations>{{adoptionLocations}}</adoption_locations>

<adverse_media>{{adverseMedia}}</adverse_media>

<entity_type>{{entityType}}</entity_type>

<due_diligence_level>{{dueDiligenceLevel}}</due_diligence_level>

<document_classifications>{{documentClassifications}}</document_classifications>

<kos_documents>{{kosDocuments}}</kos_documents>

<verification_dates>{{verificationDates}}</verification_dates>

<is_streamlined>{{isStreamlined}}</is_streamlined>

<instructions>
1. COMPLETENESS (primary, "Entity Completeness") — Read the source documents at gcs_document_path (and kos_documents, where provided). List every person, organization, and org-chart box/node they contain. Verify each one appears as a record in extracted_records (matched by nameAsSource). Any entity in the documents but missing from extracted_records is a CRITICAL Entity Completeness failure — name it and cite where it appears. Do not accept an omission on the grounds that diluted/indirect ownership falls below a threshold; every direct link must be preserved regardless of the diluted result.

2. PERCENTAGE ACCURACY (primary, "Percentage Accuracy") — For each record, check actualOwnershipPercentage (and actualVotingRightsPercentage where the document states it) against the source. It must match EXACTLY — no rounding, no assumption. A percentage that is stated in a document but recorded differently, or a percentage invented where the document gives none, is a CRITICAL Percentage Accuracy failure — cite the document value and the recorded value.

3. HALLUCINATION / NO EXTERNAL KNOWLEDGE / EVIDENCE GROUNDING ("No External Knowledge / Hallucination") — Confirm every record, every linkedName link, and every percentage traces to an explicit statement in gcs_document_path or kos_documents. Nothing may be sourced from internet lookups, public databases, or general knowledge, even if a URL appears in a document. Additionally verify every record with a non-null actualOwnershipPercentage/actualVotingRightsPercentage or a non-null linkedName carries a verbatim-quoted evidenceSnippet grounding it; a null evidenceSnippet with no corresponding gap logged in that record's outOfBounds is a grounding failure. Flag anything not traceable to a provided document as CRITICAL.

4. STRUCTURAL ("Structural Validation") — Confirm every non-null linkedName resolves to an existing record's nameAsSource, or the record is a root / ultimate parent (isUltimateParent = true). Confirm every percentage field falls within 0-100 and required schema fields are present. Confirm layer is internally consistent: client record(s) at layer 0, and every other record's layer equal to its linkedName parent's layer + 1. Confirm relationshipType comes from the section 9 list and does not contradict itemType or the cited evidence, and that a roleCapacity of "Listed Parent" (or a listed-entity stop in controlsApplied) is paired with a non-null listingProof. A broken linkedName reference, an inconsistent layer, an implausible relationshipType/roleCapacity pairing, or a "Listed Parent" record with a null listingProof is IMPORTANT.

5. REMAINING CHECKS — Evaluate the remaining criteria to the extent the schema can carry them (all are now schema-backed):
   - "Client Anchor Validation" — verify the top-level clientAnchor object: providedName echoes client_entity_name, resolvedName/clientRecordId point to the layer-0 record the set is anchored to, validationStatus is consistent with that match, and ownershipReviewStatus reflects the actual outcome. Schema-backed; a validationStatus/ownershipReviewStatus inconsistent with the evidence is IMPORTANT, and a record set anchored to a different entity than providedName is CRITICAL.
   - "Ownership Direction" — check each linkedName points the way the documents evidence (the record is an owner of / part of the linked entity, not the reverse), and no record with itemType Natural Person is recorded as owned by a Non Natural Person entity without explicit evidence. Schema-backed via linkedName and itemType; IMPORTANT on failure.
   - "Source Classification &amp; Admissibility" — using document_classifications, verify the per-record sourceClass (H1/H2/H3) is populated wherever determinable and matches the controlling source's actual class, that stronger-class evidence is not silently overridden by weaker evidence, and no evidence was dropped. Schema-backed via sourceClass; IMPORTANT on failure.
   - "Methodology (Global vs Streamlined)" — cross-check is_streamlined and adoption_locations against the top-level ownershipApproach field (GLOBAL/STREAMLINED/MIXED/NEED_REVIEW): if streamlined, confirm it was permitted and justified (controlsApplied, listingProof); if not, confirm the extraction does not look prematurely truncated. Schema-backed via ownershipApproach; IMPORTANT on failure.
   - "Local Overlay Applied" — using adoption_locations, verify countryProfileApplied reflects any stricter local threshold, countryOverrideNote explains any override of the global default, the per-record localOverlayApplied / thresholdApplied / inclusiveThreshold / drillDownRequired fields are set correctly for affected records, AND the classification outcome reflects the local threshold (section 13.6): an owner meeting a stricter/inclusive local threshold is classified isUBO/isIBO accordingly and not down-classified for being below the global default or at an inclusive boundary (e.g. exactly 25% under ≥25%). Schema-backed; IMPORTANT on failure (an omitted qualifying owner is CRITICAL under Entity Completeness).
   - "IBO / UBO Classification Accuracy" — using due_diligence_level and adoption_locations, verify isIBO / isUBO / roleCapacity against rule 12's definitions and thresholds: GLOBAL IBO &gt; 25% (CDD and EDD); UBO &gt; 25% (CDD) / &gt; 10% (EDD), unless a stricter/inclusive local threshold governs (rules 13/13.6). No qualifying owner left unflagged, no sub-threshold owner over-classified, control-based UBO counted even below the ownership threshold, and the type invariant respected (Natural Person never isIBO; Non-Natural Person never isUBO). Schema-backed; a wrong flag is IMPORTANT, an omitted qualifying owner is CRITICAL under Entity Completeness.
   - "Domination &amp; Control Indicators" — verify dominationOwnership / dominationVotingRights are captured only where explicitly evidenced, and that the per-record dominationIndicator, controlRights, ownershipVotingMismatch, and controlBasedUBOAssessmentRequired fields are consistent with the cited evidence (no domination concluded without support). Schema-backed; IMPORTANT on failure.
   - "Advisory &amp; QA Assessment" — verify the top-level advisory.records contains an entry for each material information gap evidenced in the documents (using verification_dates and due_diligence_level for context), and that qaFlags are present for material gaps and not over-emitted for routine complexity. Both halves are schema-backed; IMPORTANT on failure.
   - "Conflict Preservation" — check that where documents conflict on percentage, voting rights, or shareholder list, both versions are preserved via conflictTag rather than silently resolved. Schema-backed; IMPORTANT on failure.
   - "Overall Goal Adherence" — a surface-level, holistic check that the output as a whole is a complete, evidence-based ownership structure derived only from the provided documents, consistent with an audit-ready ownership review.

6. SCORE all 14 acceptance criteria (Pass / Fail / Mixed_Partial_Pass_Partial_Fail), using the exact criterion names from system.txt, with grounded observations (what is right/wrong, the document location, and the expected correct value). Order areas_for_improvement by severity (most critical first) and set the verdict per the verdict_rules in system.txt.
</instructions>
