<task>
Review the Ownership Extraction output against the source document. Evaluate every acceptance criterion and provide structured feedback.
</task>

<instructions>
You verify a FLAT directed ownership graph (no target, no layers, no ownership chains). Follow these steps:

1. CHART-SOURCED EDGES — for each edge with source_type == "chart_structure", verify per <chart_structure_authority> in system.txt: the node matches a chart_structure entity; an arrow exists with arrow.from == owner AND arrow.to == owned (reverse match => CRITICAL reversed direction; neither => CRITICAL hallucination); percentage matches the arrow label; direction_proof starts "From chart_structure arrow: "; UNKNOWN-confidence arrows produced no edge; LOW-confidence edges carry a data_gaps note. Apply the group-arrow, per-holding shareholder-set, per-holding sum, bracket_members, and sum_check honesty checks.

2. TEXT-SOURCED EDGES — for each edge with source_type == "document_text", verify it carries a specific page/section citation in source and direction_proof. These are NOT hallucinations. You MAY re-read the cited TEXT to confirm direction/percentage; you may NOT use the chart to refute them.

3. EMPTY chart_structure — if <chart_structure> is empty, the document is text-only: there are only document_text edges; verify them against cited text. Do NOT visually read or re-trace any chart/diagram.

4. STRUCTURAL — verify: no edge has both owner and owned as Natural Person (W1.R9); every edge references existing nodes (no dangling refs); a node in no edge has a data_gaps note; cycles are structurally correct (detected_at "W1").

5. CLASSIFICATION — verify each node's per-node entity_type_category (Cat 1/2/3 or "Entity Type not confirmed") and classification_reasoning; Category 2 exceptions; Category 3 Settlor/Trustee/Beneficiary/Protector + ARP conditions; listing_proof populated only for listed entities. Verify within-document chart-vs-text conflicts use resolution_strategy "deferred".

6. SCORE the 9 ACCEPTANCE CRITERIA (Pass / Fail / Mixed_Partial_Pass_Partial_Fail) and produce areas_for_improvement ordered by severity. Apply verdict_rules from system.txt.
</instructions>

<extraction_output>
{{extractedRecords}}
</extraction_output>

<gcs_document_path>{{gcsDocumentPath}}</gcs_document_path>

<chart_structure>{{chartStructure}}</chart_structure>