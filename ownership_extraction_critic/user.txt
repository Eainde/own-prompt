<task>
Review the Ownership Extraction output against the source document. Evaluate every acceptance criterion and provide structured feedback.
</task>

<target_entity>{{clientEntityName}}</target_entity>

<instructions>
CRITICAL PREAMBLE — CHART_STRUCTURE IS AUTHORITATIVE (W1.R12):
If <chart_structure> above is non-empty, it is the output of a dedicated Chart Reader pass that enumerated every entity, every arrow, and every percentage label visible in the document's ownership chart(s). chart_structure is the SHARED GROUND TRUTH for both you and the extractor. You MUST use chart_structure.entities and chart_structure.arrows as authoritative — do NOT re-read the chart from your own vision and trust your own reading over chart_structure.
- This breaks the vision-hallucination loop where extractor and critic both misread the chart the same way.
- For every node in the extractor's output, verify: does its name match an entry in chart_structure.entities? If NO → hallucinated entity, CRITICAL.
- For every edge in the extractor's output, verify: does there exist an arrow in chart_structure.arrows with from == edge.owner AND to == edge.owned? If NO → either the extractor invented an arrow or it reversed direction. Check whether the reverse arrow (from == edge.owned AND to == edge.owner) exists; if YES, the extractor REVERSED direction → CRITICAL (common error #5 or #16).
- For every node's spatial relationship to the target: use chart_structure.entities[target].vertical_position vs chart_structure.entities[node].vertical_position. If the node sits BELOW target, it is a SUBSIDIARY. If it appears in the extractor's output → CRITICAL.
- For each edge, the extractor must record direction_proof quoting the chart_structure arrow's evidence string ("From chart_structure arrow: '<evidence>'"). If direction_proof is fabricated prose instead of quoting chart_structure, flag as IMPORTANT.
- For each arrow with direction_confidence == UNKNOWN: the extractor should NOT have created an edge for this; check that it didn't.
- If <chart_structure> is empty (Chart Reader stage not run), perform the original visual review per the rules below.

CRITICAL PREAMBLE — TARGET ENTITY AUTHORITY (W1.R11):
The <target_entity> value above is AUTHORITATIVE. The document may be a KYC report, Periodic Review, or AFC escalation prepared FOR a DIFFERENT named client — its title, file name, email subject lines, and headers may dominate with that named client's name. Before reviewing, perform the <target_entity_authority> check in system.txt:
- Identify the document's NAMED CLIENT (from titles/email subjects/file names).
- Determine if target_entity == named_client. If NOT, determine whether named_client sits ABOVE, BELOW, UNRELATED, or NOT_IN_CHART relative to target_entity.
- If named_client is BELOW target_entity, add named_client + every chain intermediate to LIST B. The named client's textual prominence does NOT promote it to an owner.
- Record your answers in the "Ownership Direction" observations.

1. Read the ENTIRE source document first. Build TWO lists: LIST A (all entities that own the target, directly or indirectly) and LIST B (all subsidiaries/children the target owns — INCLUDING the document's named client and chain intermediates if they sit below target_entity, per the preamble). Include every intermediate holding company, investment vehicle, and person with an ownership percentage — no matter how small (no ownership threshold per W1.R3).
2. Compare LIST A against the Extractor's output — flag every missing owner. Compare LIST B against the output — flag every subsidiary that appears (it should NOT be there). Pay special attention to whether the document's named client or chain intermediates appear in nodes/edges/ownership_chain — this is the named-client trap (W1.R11 / common error #17) and is CRITICAL.
3. For each edge, verify ownership direction against the document. Check that the entity in "owner" truly holds shares in the entity in "owned". Check that the target entity never appears as "owner". Verify the control field is accurate (evidenced or null, not fabricated).
4. For each node, trace the path through edges from the target (layer 0) to that node. Verify connectivity (node is reachable from target), layer number equals hop count, and ownership_chain matches this path exactly. Flag orphan nodes with no connecting edges.
5. BRANCH ISOLATION (W1.R10): If the target has multiple layer-1 owners, verify each holding's shareholders are correctly assigned per the document. For each layer-2+ node, confirm the document shows it owning shares in the specific layer-1 entity its ownership_chain passes through. Flag any shareholder connected to a holding it doesn't own shares in (cross-branch contamination). For shared entities (same person in multiple holdings): verify one node, separate edges, valid chain.
6. For each edge, verify the percentage matches the document exactly. Check that no edge has both owner and owned as Natural Person. Flag any relationship not evidenced in the document.
7. Verify entity_type_category (including "Entity Type not confirmed" fallback), classification_reasoning, cycles (with cycle_path, detected_at, and source), conflicts (with conflict_description, resolution_strategy, resolved_value), and schema compliance. For Category 2: verify exceptions field on every node. For Category 3: verify Settlor/Trustee/Beneficiary/Protector extraction and ARP conditions.
8. Score each of the 11 acceptance criteria as Pass, Fail, or Mixed_Partial_Pass_Partial_Fail.
9. Provide actionable improvement points ordered by severity that the Extractor can directly act on.
</instructions>

<extraction_output>
{{extractedRecords}}
</extraction_output>

<gcs_document_path>{{gcsDocumentPath}}</gcs_document_path>

<chart_structure>{{chartStructure}}</chart_structure>