<task>
Review the Ownership Extraction output against the source document. Evaluate every acceptance criterion and provide structured feedback.
</task>

<target_entity>{{clientEntityName}}</target_entity>

<instructions>
1. Read the ENTIRE source document first. Build TWO lists: LIST A (all entities that own the target, directly or indirectly) and LIST B (all subsidiaries/children the target owns). Include every intermediate holding company, investment vehicle, and person with an ownership percentage — no matter how small (no ownership threshold per W1.R3).
2. Compare LIST A against the Extractor's output — flag every missing owner. Compare LIST B against the output — flag every subsidiary that appears (it should NOT be there).
3. For each edge, verify ownership direction against the document. Check that the entity in "owner" truly holds shares in the entity in "owned". Check that the target entity never appears as "owner". Verify the control field is accurate (evidenced or null, not fabricated).
4. For each node, trace the path through edges from the target (layer 0) to that node. Verify connectivity (node is reachable from target), layer number equals hop count, and ownership_chain matches this path exactly. Flag orphan nodes with no connecting edges.
5. For each edge, verify the percentage matches the document exactly. Check that no edge has both owner and owned as Natural Person. Flag any relationship not evidenced in the document.
6. Verify entity_type_category (including "Entity Type not confirmed" fallback), classification_reasoning, cycles (with cycle_path, detected_at, and source), conflicts (with conflict_description, resolution_strategy, resolved_value), and schema compliance. For Category 2: verify exceptions field on every node. For Category 3: verify Settlor/Trustee/Beneficiary/Protector extraction and ARP conditions.
7. Score each acceptance criterion as Pass, Fail, or Mixed_Partial_Pass_Partial_Fail.
8. Provide actionable improvement points ordered by severity that the Extractor can directly act on.
</instructions>

<extraction_output>
{{extractionOutput}}
</extraction_output>

<gcs_document_path>{{gcsDocumentPath}}</gcs_document_path>