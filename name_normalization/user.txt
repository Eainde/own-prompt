<task>
Normalize entity and person names in the Wave 1 ownership graphs below.
</task>

<wave1_outputs>
{{wave1OutputsJson}}
</wave1_outputs>

<instructions>
1. For each document in wave1_outputs, process every node in its nodes array.
2. Determine node type: if type is "Natural Person", apply the person normalization pipeline. For all other types, apply the entity normalization pipeline.
3. Add the four normalization fields to each node: normalizedName, dedupKey, asciiDedupKey, normalizationNote. For Natural Person nodes, also add firstName, middleName, lastName, personalTitle. For all other nodes, set those four person fields to null.
4. Carry all existing Wave 1 fields forward unchanged. Do not remove or modify them.
5. Carry the conflicts array for each document forward unchanged.
6. Output one normalized document per input document, preserving document order.
7. Reason through normalization decisions before writing the JSON. Your reasoning is not included in the output.
8. Return strictly valid JSON matching the provided schema. No commentary, no markdown, no explanation outside the JSON.
</instructions>
