<task>
Normalize entity and person names in the Wave 1 ownership graphs below.
- The definition of too big is when the number of elements under the 'nodes' field is more than 200.
- Do not truncate the output unless instructed by the presence of submitBatch tool.
</task>

<extracted_records>
{{extractedRecords}}
</extracted_records>

<instructions>
1. For each document in wave1_outputs, process every node in its nodes array.
2. Determine node type: if type is "Natural Person", apply the person normalization pipeline. For all other types, apply the entity normalization pipeline.
3. Add the four normalization fields to each node: normalizedName, dedupKey, asciiDedupKey, normalizationNote. For Natural Person nodes, also add firstName, middleName, lastName, personalTitle. For all other nodes, set those four person fields to null.
4. Carry all existing Wave 1 node fields (id, name, type, entity_type_category, listing_proof, data_gaps, exceptions, classification_reasoning) forward unchanged on each node. Do not remove or modify them.
5. Carry the edges (including source_type and direction_proof), cycles, and conflicts arrays for each document forward completely unchanged.
6. Output one normalized document per input document, preserving document order.
7. Reason through normalization decisions before writing the JSON. Your reasoning is not included in the output.
8. Return strictly valid JSON matching the provided schema. No commentary, no markdown, no explanation outside the JSON.
9. If the submitBatch tool is available and your nodes array exceeds 200 elements, split the output into batches via submitBatch with primaryField "nodes". Each batch must be valid JSON matching the schema, containing a subset of nodes with their corresponding edges, cycles, and conflicts. Preserve node id slugs as-is across batches. After the last batch, return ONLY a text summary — no JSON.
</instructions>
