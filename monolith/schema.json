{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Ownership Structure Candidates Schema",
  "description": "Schema for ownership structure chart candidates with appropriate reasons.",
  "type": "object",
  "required": ["extracted_records", "outOfBounds", "qaFlags"],
  "properties": {
    "extracted_records": {
      "type": "array",
      "description": "Organization and individual candidates to build ownership structure chart.",
      "items": {
        "type": "object",
        "required": [
          "id",
          "itemType",
          "nameAsSource",
          "linkedName",
          "pageNumber",
          "documentName",
          "isUltimateParent",
          "isIBO",
          "isUBO",
          "actualOwnershipPercentage",
          "actualVotingRightsPercentage",
          "accumulatedOwnership",
          "accumulatedVotingRights",
          "confidenceScore",
          "governanceBasis",
          "canonicalReasonTokens",
          "temporalStatus",
          "countryProfileApplied",
          "countryOverrideNote",
          "scopeTag",
          "currencyTag",
          "coverageMode",
          "transparencyCode",
          "controlsApplied",
          "negativeSignals",
          "qaFlags",
          "outOfBounds"
        ],
        "properties": {
          "id": {
            "type": "integer",
            "minimum": 1,
            "description": "Sequential integer, 1-based, in document reading order."
          },
          "itemType": {
            "type": "string",
            "enum": ["INDIVIDUAL", "ORGANIZATION"],
            "description": "INDIVIDUAL when item is derived from an individual input. ORGANIZATION when item is derived from an organization input."
          },
          "nameAsSource": {
            "type": "string",
            "description": "Original name."
          },
          "linkedName": {
            "type": "string",
            "nullable": true,
            "description": "Name of Linked/Parent Organization."
          },
          "pageNumber": {
            "type": "integer",
            "minimum": 1
          },
          "documentName": {
            "type": "string",
            "description": "Name of the document from which this record was extracted."
          },
          "isUltimateParent": {
            "type": "boolean",
            "description": "True if this record is the ultimate parent of the ownership structure."
          },
          "isIBO": {
            "type": "boolean",
            "description": "IBO is true if an IBO of the organisation else false."
          },
          "isUBO": {
            "type": "boolean",
            "description": "UBO is true if an UBO of the organisation else false."
          },
          "actualOwnershipPercentage": {
            "type": "number",
            "minimum": 0.0,
            "maximum": 100.0,
            "description": "Actual ownership percentage of the individual or organization in the entity."
          },
          "actualVotingRightsPercentage": {
            "type": "number",
            "minimum": 0.0,
            "maximum": 100.0,
            "description": "Voting rights percentage of the individual or organization in the entity."
          },
          "accumulatedOwnership": {
            "type": "number",
            "minimum": 0.0,
            "maximum": 100.0,
            "description": "Accumulated ownership of the individual or organization in the entity."
          },
          "accumulatedVotingRights": {
            "type": "number",
            "minimum": 0.0,
            "maximum": 100.0,
            "description": "Accumulated voting rights of the individual or organization in the entity."
          },
          "dominationOwnership": {
            "type": "number",
            "minimum": 0.0,
            "maximum": 100.0,
            "description": "Domination ownership of the individual or organization in the entity."
          },
          "dominationVotingRights": {
            "type": "number",
            "minimum": 0.0,
            "maximum": 100.0,
            "description": "Domination voting rights of the individual or organization in the entity."
          },
          "conflictTag": {
            "type": "string",
            "enum": ["C: clear", "C: resolved", "C: unresolved"],
            "description": "Provide if conflict detected. C: clear = no conflict detected. C: resolved = conflict detected but resolved. C: unresolved = conflict detected but not resolved."
          },
          "confidenceScore": {
            "type": "number",
            "minimum": 0.0,
            "maximum": 1.0,
            "description": "Deterministic confidence score computed per 5D scoring rules. Formula: round(clamp(sum(positives) - sum(negatives), 0, 1) + consensus_multiplier, 2). Threshold: 0.70 for inclusion."
          },
          "governanceBasis": {
            "type": "string",
            "description": "7-part structured reason. Format: [1] GOVERNANCE ROLE: role per rule_id. [2] SOURCE BASIS: sourceClass and distribution. [3] ENTITY SCOPE: binding status and entity type. [4] KDB ROLE INTERPRETATION: how role maps to IBO/UBO under applicable rules. [5] CURRENCY and QUALITY: temporal status and source quality. [6] SIGNING NARRATIVE: signatoryType and token weight. [7] FINAL DECISION: isIBO/isUBO, rules applied, confidence, transparencyCode."
          },
          "canonicalReasonTokens": {
            "type": "string",
            "description": "Machine-checkable reason tokens. Format: 'GOV={basis} SRC={tier} ESEL={status} C={conflict} U={currency} T=0.70({met/not_met}) MODE={ALL/CUR}'. Example: 'GOV=exec_body SRC=H1 ESEL=binding C=clear U=current T=0.70(met) MODE=ALL'."
          },
          "temporalStatus": {
            "type": "string",
            "enum": ["current", "former", "unknown"]
          },
          "countryProfileApplied": {
            "type": "string",
            "description": "'CP-XX' code or null."
          },
          "countryOverrideNote": {
            "type": "string",
            "description": "Override explanation or null."
          },
          "scopeTag": {
            "type": "string",
            "description": "'S: branch' or null."
          },
          "currencyTag": {
            "type": "string",
            "description": "'U: low authority source' or null."
          },
          "transparencyCode": {
            "type": "string",
            "enum": ["CONFIDENT", "NOT_CONFIDENT", "DOC_INVALID", "CONFLICT_VS", "COGNATE_MIS", "Q_STALE", "Q_CHANGING", "FORMER_ONLY", "MKF_APPLIED"],
            "description": "Structured transparency code. CONFIDENT = high confidence. NOT_CONFIDENT = ambiguous. DOC_INVALID = source issue. CONFLICT_VS = source conflict. COGNATE_MIS = cognate name mismatch concern. Q_STALE = outdated source (>3yr). Q_CHANGING = registry in flux. FORMER_ONLY = all roles departed. MKF_APPLIED = executive body override used."
          },
          "coverageMode": {
            "type": "string",
            "enum": ["ALL"],
            "description": "Coverage mode. ALL = all individuals/organisations emitted regardless of currency. Always ALL in current pipeline."
          },
          "negativeSignals": {
            "type": "array",
            "items": { "type": "string" },
            "description": "List of negative signals detected. Empty array if none. Format: 'former-only roles (-0.40)', 'single H4 source (-0.30)'."
          },
          "controlsApplied": {
            "type": "array",
            "items": { "type": "string" },
            "description": "Control rules checked or applied for this candidate. Include rules checked or triggered. Omit rules structurally irrelevant to this candidate's entity type or country."
          },
          "qaFlags": {
            "type": "array",
            "items": { "type": "string" },
            "description": "QA annotation flags for downstream audit and review. Each entry is a free-text flag from QA rules. Empty array when no issues detected."
          },
          "outOfBounds": {
            "type": "object",
            "description": "Out-of-bounds assessment derived from ownership results.",
            "required": ["isOutOfBounds"],
            "properties": {
              "isOutOfBounds": {
                "type": "boolean",
                "description": "True if this individual or organization is determined to be out of bounds for ownership candidacy based on ownership results."
              },
              "reason": {
                "type": "string",
                "description": "Canonical reason for OOB flag. Null when isOutOfBounds is false."
              },
              "status": {
                "type": "string",
                "enum": ["NEEDS_REVIEW", "DOC_INVALID", "COUNTRY_OUT_OF_SCOPE", "DOCUMENT_NOT_RELEVANT"],
                "description": "OOB status category. Set to null when isOutOfBounds is false."
              }
            }
          }
        }
      }
    },
    "outOfBounds": {
      "type": "object",
      "nullable": true,
      "description": "A summary and list of records that were identified but fall outside the defined scope of an Ownership Structure.",
      "required": ["summary", "documents", "records"],
      "properties": {
        "summary": {
          "type": "string",
          "nullable": true,
          "description": "Start with the documentName and then give an overall summary of the out-of-bounds records (avoid count of records) including firstName, lastName or legalName (if available), documentName (mention the documentName in this summary), reason (if applicable) including the status."
        },
        "documents": {
          "type": "array",
          "nullable": true,
          "description": "A list of documents that were identified but fall outside the defined scope of an Ownership Structure.",
          "items": {
            "type": "object",
            "required": ["fileName", "reason", "status"],
            "properties": {
              "fileName": { "type": "string", "description": "A unique fileName." },
              "reason": { "type": "string", "description": "A clear explanation of why this file record is considered out of bounds." },
              "status": { "type": "string", "enum": ["DOC_INVALID", "DOCUMENT_NOT_RELEVANT"], "description": "The status of the reason, indicating DOC_INVALID or DOCUMENT_NOT_RELEVANT." }
            }
          }
        },
        "records": {
          "type": "array",
          "nullable": true,
          "description": "A list of items that were identified but fall outside the defined scope of an Ownership Structure.",
          "items": {
            "type": "object",
            "required": ["id", "reason", "status"],
            "properties": {
              "id": { "type": "integer", "description": "A unique, auto-generated incremental ID for this record." },
              "reason": { "type": "string", "description": "A clear explanation of why this record is considered out of bounds." },
              "status": { "type": "string", "enum": ["NEED_REVIEW", "COUNTRY_OUT_OF_SCOPE"], "description": "The status of the reason, indicating NEED_REVIEW or COUNTRY_OUT_OF_SCOPE." }
            }
          }
        }
      }
    },
    "qaFlags": {
      "type": "object",
      "nullable": false,
      "description": "A summary and set of quality assurance and inference flags based on the document analysis.",
      "required": ["summary", "records"],
      "properties": {
        "summary": {
          "type": "string",
          "nullable": true,
          "description": "Start with the documentName and then give an overall summary of the qa_flags records."
        },
        "records": {
          "type": "array",
          "nullable": true,
          "description": "A list of quality assurance and inference flag reasons based on the document analysis.",
          "items": {
            "type": "object",
            "required": ["reason", "status"],
            "properties": {
              "reason": { "type": "string", "description": "A clear explanation of why this reason has quality assurance and inference flags." },
              "status": { "type": "string", "enum": ["CONFIDENT", "NOT_CONFIDENT"], "description": "The status of the record, indicating CONFIDENT or NOT_CONFIDENT." }
            }
          }
        }
      }
    }
  }
}
