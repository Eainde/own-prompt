<task>
Combine the normalized ownership documents below into a single composite annotated graph. Do NOT merge or remove any node or edge.
- The definition of too big is when the number of elements under the 'nodes' field is more than 200.
- Do not truncate the output unless instructed by the presence of submitBatch tool.
</task>

<normalised_entities>
{{normalisedEntities}}
</normalised_entities>

<instructions>
1. Collect all nodes from every normalized document. For each node, assign a namespaced id of the form <source_doc_id>::<original_slug> where source_doc_id is the document identifier (e.g. doc_1, doc_2, or a filename slug). Set source_document on every node to the originating document id. Carry forward all other node fields (name, normalizedName, dedupKey, asciiDedupKey, type, data_gaps, exceptions, listing_proof, classification_reasoning) unchanged.
2. Collect all edges from every document. Remap each edge's owner and owned to the namespaced ids of the SAME document that edge came from (e.g. doc_1::alpha_holdings). Set source_document on every edge. Carry forward source_type, ownership_percentage_direct, control, and direction_proof unchanged. Do NOT merge edges across documents.
3. Build the overlaps array per W3.R3: group namespaced node ids that are candidate same-entity matches. Group first by exact dedupKey, then by asciiDedupKey, then by unambiguous normalizedName similarity within the same entity type. For each group emit: dedup_key, member_ids, match_basis ("dedupKey" | "asciiDedupKey" | "similarity"), confidence_note. Omit singleton groups (no cross-document candidates). This is an annotation only — do NOT merge the members.
4. Build the potential_conflicts array per W3.R4: for each pair of overlap groups (owner_overlap, owned_overlap) that share edges across documents, compare ownership_percentage_direct values. Emit one entry with owner_overlap (the dedup_key of the owner group), owned_overlap (the dedup_key of the owned group), values array (all observed percentages), sources array (corresponding source documents), and status "agree" if all values are within 2% of each other, else "contradict". Do NOT set resolution_strategy or resolved_value.
5. Carry the upstream cycles and conflicts arrays forward UNCHANGED per W3.R5. Do not modify, resolve, or add to either array.
6. Return strictly valid JSON matching the provided schema. No commentary, no markdown, no explanation outside the JSON.
7. If the submitBatch tool is available and your nodes array exceeds 200 elements, split the output into batches via submitBatch with primaryField "nodes". Each batch must be valid JSON matching the schema, containing a subset of nodes with their corresponding edges, overlaps, potential_conflicts, cycles, and conflicts. Node ids are namespaced slugs (e.g. doc_1::alpha_holdings) — preserve them exactly across batches. After the last batch, return ONLY a text summary — no JSON.
</instructions>
